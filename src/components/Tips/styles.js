import styled from 'styled-components';

export const TipsDot = styled.div`
  font-size: 12px;
  color: #fff;
  border-radius: 999px;
  border: 1px solid #fff;
  padding: 5px 20px;
  text-transform: uppercase;
  cursor: pointer;
  backdrop-filter: blur(1rem);

  &:hover {
    background: #fff;
    color: #000;
  }

`;
